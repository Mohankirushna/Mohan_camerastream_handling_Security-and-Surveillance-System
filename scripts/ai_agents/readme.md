Just run the script, its generates captions for every image, if the caption has any keyword which can be mapped to a crime, the model tell crime detected, can be later chagned to return 0 or 1 to indicate if crime was detected or not, the model can only take in 1 image at a time so far. Run the script while defining where to save the pretrained model at if needed or comment it out.